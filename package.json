{
  "name": "fathom-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.9",
    "typescript": "^5.9.3"
  }
}
